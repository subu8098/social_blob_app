<!DOCTYPE html>
<html>
  <head>
    <title>SocialApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

 <body style="font-family: Arial, sans-serif; background-color: #f7f7fa;">
  <nav style="background: #232F3E; padding: 10px 20px; display: flex; align-items: center;">
    <span style="color: #fff; font-weight: bold; font-size: 1.2em; margin-right: 30px;">Social App</span>
    <% if current_user %>
      <a href="<%= profile_path %>" style="color: #fff; margin-right: 20px; text-decoration: none;">Profile</a>
      <a href="<%= posts_path %>" style="color: #fff; margin-right: 20px; text-decoration: none;">Feed</a>
      <a href="<%= users_path %>" style="color: #fff; margin-right: 20px; text-decoration: none;">Explore Users</a>
      <%= button_to "Logout", logout_path, method: :delete, form: { style: "display: inline;" }, style: "background: #DD4B39; cursor: pointer; color: #fff; border: none; padding: 6px 12px; border-radius: 3px; margin-right: 20px;" %>
      <span style="color: #fff; margin-left: auto;">Logged in as:   <b style="color: #f04747ff;"><%= current_user.username %></b></span>
    <% else %>
      <a href="<%= login_path %>" style="color: #fff; margin-right: 20px; text-decoration: none;">Login</a>
      <a href="<%= signup_path %>" style="color: #fff; margin-right: 20px; text-decoration: none;">Sign Up</a>
    <% end %>
  </nav>
  <div style="max-width: 900px; margin: 30px auto; background: #fff; box-shadow: 10px 20px 60px #ddd; border-radius: 8px; padding: 20px;">
    <%= yield %>
  </div>
</body>

</html>
